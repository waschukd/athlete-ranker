// Single source of truth for the Anthropic model used across the app's AI
// features (scouting, Coach's Read compare-analysis, smart schedule import).
// Change here to swap the model everywhere.
export const AI_MODEL = "claude-fable-5";
